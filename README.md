# Щоденний облік

Локальний Windows-віджет для щоденного обліку запитів працівників. Програма працює без мережі, не має телеметрії та зберігає робочі дані лише на комп'ютері користувача.

## Уже закладена логіка

- до 15 активних працівників у компактному круглому віджеті без прямокутної рамки;
- довільне переміщення віджета та зміна його розміру від 260 до 700 пікселів зі збереженням позиції;
- один запит зазвичай закриває один робочий день;
- складний запит за окремим дозволом може закривати два робочі дні;
- перша одиниця закриває поточний день, решта — найближчі попередні незакриті дні, рухаючись назад від учора;
- нерозподілений залишок можна за окремим підтвердженням зарахувати назад або наперед;
- майбутні дні ніколи не закриваються автоматично: програма запитує окреме підтвердження;
- о 18:00 незаповнений робочий день автоматично отримує статус «Не подав»;
- окремі статуси для інших завдань, особистих справ, лікарняного, відпустки та відгулу;
- субота й неділя автоматично позначаються «ВХ», але конкретний вихідний можна вручну зробити робочим днем для працівника;
- табель за місяць, текстова аналітика, підрахунок відпрацьованих днів;
- окремий модуль чергувань: по двоє щодня, включно з вихідними, один черговий лише за явним дозволом;
- окремий вибір учасників графіка без обов’язкового включення всіх працівників програми;
- швидке ручне заповнення: лівий клік ставить або знімає чергування, правий відкриває «А», відсутності та реалізоване чергування;
- кнопка формування планує тільки наступний календарний тиждень, використовуючи вручну внесену історію;
- справедливий генератор із початковими історичними підсумками, окремим обліком реалізованих чергувань, обмеженнями «А» та відсутностями;
- повний імпорт/експорт резервної копії JSON і табличний експорт CSV.

Відпрацьованими вважаються дні, закриті запитом, і дні зі статусом «Залучений до інших завдань». Особисті справи, лікарняний, відпустка, відгул та святкові дні показуються окремо і не збільшують лічильник відпрацьованих днів.

У графіку чергувань календарні вихідні залишаються звичайними днями графіка. Працівник не потрапляє до розподілу, якщо його виключено зі складу учасників або для нього позначено вихідний, відпустку, лікарняний, відгул, особисті справи чи «А». Позначка «А» блокує і сам день, і наступний. Генератор насамперед вирівнює загальну кількість чергувань, використовує реалізовані чергування як додатковий показник навантаження та за достатньої кількості доступних людей залишає щонайменше два повних дні між чергуваннями однієї людини.

## Запуск для розробки

Потрібен Node.js 24 або новіший.

```bash
npm install
npm test
npm start
```

## Збірка для Windows

```bash
npm install
npm run dist:win
```

У каталозі `dist` буде створено переносимий файл `Counter.exe`, який не потребує інсталятора. У GitHub Actions він завантажується як артефакт `Counter`.

## Зберігання даних

Основний файл бази створюється в системному каталозі даних застосунку Electron. Запис виконується атомарно, поруч зберігається попередня резервна копія. Для перенесення даних між комп'ютерами слід використовувати експорт та імпорт JSON.
